
archive:
  remote: https://github.com/open-source-parsers/jsoncpp/archive/1.9.1.tar.gz
  local: jsoncpp-1.9.1.tar.gz

build:
  type: cmake
  # command: cmake --DCMAKE_INSTALL_PREFIX=${PREFIX_ROOT} -DCMAKE_TOOLCHAIN_FILE=${TOOLCHAIN}.cmake

